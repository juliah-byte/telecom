server:
  port: 62345
spring:
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/telecom_mobile
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
logging:
  level:
    org: 
    springframework:
      web: debug
      
        
      
    